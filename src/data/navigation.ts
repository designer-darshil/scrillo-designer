import { defaultWebsiteData } from './defaultWebsiteData';

export const navigationItems = defaultWebsiteData.header!.navigationItems;
export const socialLinks = defaultWebsiteData.footer.socialLinks;

