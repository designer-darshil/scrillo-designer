import { Project } from '../types';
import { defaultWebsiteData } from './defaultWebsiteData';

export type { Project };

export const projectsData: Project[] = defaultWebsiteData.projects;

export default projectsData;

